# Gate 2 — verification evaluation

> **SYNTHETIC_EVALUATION.** Gate 2 false-acceptance and false-rejection counts measured on the generated dataset — seed `20260821`, 535 days, 6 injected events (`python -m data.generate`). The events were injected by this repository, so their ground truth is known by construction rather than labelled by a human. These figures characterise the method on this dataset; they are **not** production accuracy and do not predict performance on real business data.
>
> The corrupt narratives are hand-written to exercise known violation classes. Zero false acceptance means none of THOSE got through — not that no corrupt narrative could.

Generated by `python -m eval.run_verification_eval`.

Gate 2 is deterministic and calls no model. Same bundle plus same narrative produces the same report, including the order of the violation list.

## Headline

| Metric | Value | Target |
|---|---:|---:|
| **False acceptance** (invalid narrative passed) | **0** | 0 |
| False rejection (valid narrative blocked) | 0 of 6 | 0 |
| Injected violations identified by expected code | 9 / 9 | all |
| Total checks executed | 150 | |
| Checks passed | 120 (80.0%) | |

No invalid narrative was accepted. That is the only result on this page that would matter if it were different: a blocked valid narrative costs a retry, while an accepted invalid one reaches a human as a false statement carrying the system's authority.

## Invalid narratives

Hand-written, one lie each, constructed independently of the checker. A verifier tested only against narratives its own code produced is a verifier tested against itself.

| Case | The lie | Expected code | Identified | Blocked |
|---|---|---|:--:|:--:|
| A | invented number | `UNGROUNDED_NUMBER` | yes | yes |
| B | wrong driver | `UNKNOWN_DRIVER` | yes | yes |
| C | wrong direction | `DIRECTION_MISMATCH` | yes | yes |
| D | missing evidence | `MISSING_EVIDENCE` | yes | yes |
| E | invalid evidence id | `INVALID_EVIDENCE_ID` | yes | yes |
| F | dominant driver omitted | `DOMINANT_DRIVER_OMITTED` | yes | yes |
| G | unsupported causal claim | `CAUSAL_LANGUAGE_NOT_LICENSED` | yes | yes |
| I | unauthorised evidence id | `INVALID_EVIDENCE_ID` | yes | yes |
| J | invented lever | `UNKNOWN_LEVER` | yes | yes |

## Valid narratives

| Case | Description | Passed | Hard violations |
|---|---|:--:|---:|
| V-S1 | deterministic narrative, S1 | yes | 0 |
| V-S2 | deterministic narrative, S2 | yes | 0 |
| V-S3 | deterministic narrative, S3 | yes | 0 |
| V-S6 | deterministic narrative, S6 | yes | 0 |
| H | licensed causal claim | yes | 0 |
| V-assoc | associative phrasing on a conflicted case | yes | 0 |

## Violations by type

| Code | Severity | Count |
|---|---|---:|
| `UNUSED_EVIDENCE` | INFO | 9 |
| `DOMINANT_DRIVER_OMITTED` | HARD | 8 |
| `UNGROUNDED_NUMBER` | HARD | 2 |
| `INVALID_EVIDENCE_ID` | HARD | 2 |
| `UNKNOWN_LEVER` | HARD | 2 |
| `UNKNOWN_DRIVER` | HARD | 1 |
| `DIRECTION_MISMATCH` | HARD | 1 |
| `MISSING_EVIDENCE` | HARD | 1 |
| `CAUSAL_LANGUAGE_NOT_LICENSED` | HARD | 1 |

## Severity policy

Hard violations block delivery. Each one is a case where a wrong answer is worse than no answer.

| Code | Why it is HARD |
|---|---|
| `UNGROUNDED_NUMBER` | a figure with no matching metric fact came from nowhere the system can point at, and a wrong number stated confidently is worse than no number |
| `UNGROUNDED_DATE` | a date outside the analysis window or the evidence it cites places the movement somewhere it did not happen |
| `UNKNOWN_DRIVER` | naming a driver the analysis never produced invents a finding |
| `DIRECTION_MISMATCH` | saying a metric rose when it fell inverts the entire conclusion, and the reader has no way to catch it |
| `MISSING_EVIDENCE` | a substantive claim with no citation is an assertion, which is the thing this layer exists to prevent |
| `INVALID_EVIDENCE_ID` | a citation that resolves to nothing is indistinguishable from a fabricated one |
| `RESTRICTED_EVIDENCE` | citing a source the persona may not read leaks it, whether or not the text quotes its contents |
| `DOMINANT_DRIVER_OMITTED` | narrating a weaker explanation while omitting the strongest one is misleading by selection rather than by statement |
| `CAUSAL_LANGUAGE_NOT_LICENSED` | asserting causation the counterfactual never established is the specific error the causal gate was built for |
| `UNKNOWN_LEVER` | an action nobody approved must never reach a human as a recommendation |
| `INVALID_METRIC_REF` | a metric reference that resolves to nothing cannot be checked, so the number it carries cannot be trusted |

SOFT (correctable on retry): `MISSING_CAVEAT`, `HEDGING_INCONSISTENT`, `PERSONA_DEPTH`.

INFO (logged, never blocks): `UNUSED_EVIDENCE`.

## Limitations

- **Direction and causal checks match a fixed vocabulary.** A sentence that conveys a rise without using any of the rise words passes the direction check unexamined. This is deliberate — a semantic judge would be a second model whose errors nobody can audit — but it is a real ceiling, and paraphrase defeats it.
- **The numeric allowlist admits cohort figures as well as metric facts.** Both are frozen into the bundle and computed deterministically, so neither can be invented, but the allowlist is wider than `metric_facts` alone.
- **Structural integers 0-3 are allowed unconditionally**, so a narrative could state a wrong small count. No business figure in this system is a bare single digit, and requiring a metric fact for the `1` in `#1` would make every ranked list unverifiable.
- **Entitlement is checked against the frozen bundle**, which already excludes restricted sources. The check is a second line, not the primary control; the primary control is Stage 5 refusing the read at the gateway.
- **The advisory LLM judge (Part 13.2, check 10) is not built.** It is advisory by design and would require a model call, which this stage does not make.
- **No retry loop.** Part 6.4's failure ladder specifies one re-narration attempt before falling back to the template. There is nothing to re-narrate yet, so only the template exists.

## Commands

```bash
python -m eval.run_verification_eval
```

```bash
python -m eval.demo_verification
```

